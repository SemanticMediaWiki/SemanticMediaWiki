{{!
  EntityExaminer separator line. Serves both the indicator composite line
  and the elastic indexer line (byte-identical source templates).

  Required data:
  - margin: string (escaped) - Margin side, left or right
}}
<div style="border-top: 1px solid #ebebeb;margin-top: 10px;margin-bottom: 8px;margin-{{margin}}: -10px;width: 280px;"></div>
