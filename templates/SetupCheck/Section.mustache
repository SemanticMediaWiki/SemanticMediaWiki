{{!
  Section heading for the SetupCheck error page.

  Required data:
  - data-template: string (escaped) - template marker, must survive in output as an HTML comment
  - text: string (escaped) - plain section heading message
}}
<!--{{data-template}}--><h3 class="section"><span class="title">{{text}}</span></h3>
